spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/timairport_db
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create



custom:
  prop: "salut"